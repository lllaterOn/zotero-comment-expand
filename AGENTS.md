# Zotero Comment Expand

这是独立的 Zotero 桌面插件。只控制阅读器批注评论的完整/省略显示，不改变批注原文或数据。首次安装默认关闭，开关作为本机全局偏好保存。

## 修改约定

- 保持功能单一，不引入内容同步、外部服务或其他插件依赖。
- 评论按钮位于原生搜索组内、搜索左侧，工具条保持单行；保留其他插件自己的滚动行为。
- 不改写原生搜索、其他插件的节点或批注内容。新增样式和节点使用独立标识，并在停用时清理。
- Reader 的内部 DOM/CSS 访问集中在 `src/reader.ts`；变动时核对支持版本的实现。
- 使用分支和 pull request 维护后续改动，合并前运行 `npm run verify`，并按改动范围补充必要的实机验收。
- Zotero 实机验收由维护者执行。没有明确授权，不启动隔离实例，也不安装到维护者正在使用的配置。
- 不将 Zotero 数据库、配置、个人路径、凭证、日志、缓存或安装包提交到源码仓库。

## 发布约定

- 版本使用 `major.minor.patch`，标签使用 `v<version>`，同步更新包清单、锁文件和变更记录。
- 标签工作流生成 Draft Release 和 SHA256；维护者验收后发布同一份资产，不覆盖已发布版本。
- 发布后检查 `updates.json` 的版本、下载地址和 SHA256。只有正式版本进入更新清单。
- 具体命令与验收要求见 `docs/DEVELOPMENT.md`。
